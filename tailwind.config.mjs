/**
 * O Tailwind CSS v4 usa configuração CSS-first em src/styles/global.css.
 * Este arquivo permanece disponível para futuras extensões que precisem de JS.
 */
export default {
  theme: {
    extend: {},
  },
};
